import React, { Component } from "react";
import Header from "./Header";

class Post extends Component {
  render() {
    return (
      <div>
        <Header />
        Post</div>
    );
  }
}

export default Post;